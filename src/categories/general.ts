import { Category } from '../models/category'

export default new Category({
  name: 'General',
  description:
    'Default commands group to make some fun, some utils and other aspects possible',
  emoji: '📢',
})
