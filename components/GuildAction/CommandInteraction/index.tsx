import { GuildActionProps } from '@components/GuildAction'

const CommandInteraction: React.FC<GuildActionProps> = ({ guild: guildId, action: actionId }) => {
  return <>TODO: settings of {actionId}</>
}

export default CommandInteraction
